<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/index.css" />
    <title><%= title %></title>
  </head>
  <body>
    <% if (user) { %> <% if (user.membership) { %>
    <h2>
      Welcome member <%= user.first_name %> to our exclusive Hot Takes clubhouse
    </h2>
    <% } else { %>
    <h2>Welcome <%= user.first_name %> to our exclusive Hot Takes clubhouse</h2>
    <% } %> <% } %>

    <div class="messages-container">
      <% messages.forEach(msg => { %>
      <div class="message-card">
        <p class="message-content"><%= msg.content %></p>
        <div class="message-meta">
          <span class="message-date">
            <%= new Date(msg.created_at).toLocaleString() %>
          </span>
          <span class="message-likes">❤️ <%= msg.likes %></span>
          <% if (user && user.membership) { %>
          <span class="message-author">— <%= msg.first_name %></span>
          <% } %>
        </div>
      </div>
      <% }) %>
    </div>
  </body>
</html>
