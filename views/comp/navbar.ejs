<nav class="navbar">
  <div class="navbar-left">
    <a href="/">
      <img src="/img/icon.png" alt="Logo" class="navbar-logo" />
    </a>
    <ul class="navbar-menu">
      <% if (!user) { %>
      <li><a href="/sign-up">Sign Up</a></li>
      <li><a href="/login">Login</a></li>
      <% } else { %> <% if (!user.membership) { %>
      <li><a href="/join-club">Join the Club</a></li>
      <% } else { %>
      <li><a href="/newmsg">New Hot Take</a></li>
      <% } %>
      <li><a href="/logout">Logout</a></li>
      <% } %> <% if (user && !user.is_admin) { %>
      <li><a href="/admin">Become Admin</a></li>
      <% } else if (user && user.is_admin) { %>
      <li><a href="#" style="color: var(--highlight-color)">Admin</a></li>
      <% } %>
    </ul>
  </div>
</nav>
